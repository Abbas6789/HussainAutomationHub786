# ============================================================
#  Filename: requirements.txt
#  Part: 1 — Public Website Files (also used by admin)
#  Developer: Ghulam Hussain
# ============================================================
#
#  Only streamlit is needed — all other dependencies
#  (sqlite3, hashlib, uuid, urllib) are Python stdlib.
#  Firebase calls use Python's built-in urllib (no SDK needed).
#
#  Install:  pip install -r requirements.txt
#  Deploy:   Streamlit Cloud reads this file automatically.
# ============================================================

streamlit>=1.35.0
